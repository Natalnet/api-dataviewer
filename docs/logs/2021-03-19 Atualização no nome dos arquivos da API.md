# Atualização no nome dos arquivos da API

Visando dados futuros e estabelecer um padrão, o arquivo handler (a API), que na versão para db foi chamado de handler_db durante os testes, passará a se chamar handler_lop, 
porque essa API gerá apenas dados relacionados ao LoP. Num futuro que tiver integreção com outras plataformas, elas deveram ser chamadas no prefixo do handler a fim de manter
um padrão.
